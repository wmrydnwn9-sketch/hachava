import { createServerSupabase } from '@/lib/supabase/server';
export default async function AboutPage(){ const supabase = await createServerSupabase(); const { data: settings } = await supabase.from('site_settings').select('*').single(); return <section className="mx-auto max-w-5xl px-4 py-12"><div className="rustic-card rounded-4xl p-8 md:p-10"><p className="mb-3 text-sm tracking-[0.3em] text-amber-100">משפחתיות • טריות • פרימיום • כשר</p><h1 className="text-4xl font-black gold-text">{settings?.about_title ?? 'אודות החווה'}</h1><div className="mt-6 space-y-6 text-lg leading-9 text-stone-100"><p>{settings?.about_body}</p><p>אנחנו מקפידים על איכות בשר גבוהה, טריות יומיומית, שירות אישי וחוויית קנייה חמה ונעימה.</p><p>הגישה שלנו פשוטה: חומרי גלם מעולים, טיפול מקצועי, כשרות ברורה וחיבור אמיתי לערך של "מהמשק לצלחת".</p></div></div></section>; }
