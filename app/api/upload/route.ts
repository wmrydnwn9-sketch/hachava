import { NextResponse } from 'next/server';
import { assertAdminApi } from '@/lib/auth';
import { supabaseAdmin } from '@/lib/supabase/admin';
export async function POST(request: Request){ if(!(await assertAdminApi())) return NextResponse.json({ error:'Unauthorized' },{ status:401 }); const formData = await request.formData(); const file = formData.get('file') as File | null; const folder = String(formData.get('folder') || 'general'); if(!file) return NextResponse.json({ error:'Missing file' },{ status:400 }); const bytes = await file.arrayBuffer(); const buffer = Buffer.from(bytes); const safeName = `${Date.now()}-${file.name.replace(/\s+/g,'-')}`; const path = `${folder}/${safeName}`; const { error } = await supabaseAdmin.storage.from('site-assets').upload(path, buffer, { contentType:file.type, upsert:false }); if(error) return NextResponse.json({ error:error.message },{ status:500 }); const { data } = supabaseAdmin.storage.from('site-assets').getPublicUrl(path); return NextResponse.json({ ok:true, path, url:data.publicUrl }); }
