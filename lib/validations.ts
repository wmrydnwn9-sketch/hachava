import { z } from "zod";
export const orderSchema = z.object({ customer_name: z.string().min(2, "יש להזין שם מלא"), phone: z.string().min(8, "יש להזין מספר טלפון תקין"), address: z.string().optional(), notes: z.string().optional(), order_type: z.enum(["delivery", "pickup"]), items: z.array(z.object({ product_id: z.string().uuid(), quantity: z.number().int().positive() })).min(1, "יש לבחור לפחות מוצר אחד") });
export const promotionSchema = z.object({ title_he: z.string().min(2), description_he: z.string().min(2), image_url: z.string().url().optional().or(z.literal("")), is_active: z.boolean(), starts_at: z.string().nullable().optional(), ends_at: z.string().nullable().optional() });
