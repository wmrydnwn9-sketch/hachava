import * as React from "react";
import { Html, Body, Container, Heading, Text, Section } from "@react-email/components";
export function NewOrderEmail(props: { customerName: string; phone: string; address?: string; notes?: string; orderType: "delivery" | "pickup"; items: Array<{ name: string; quantity: number }>; }) { return <Html lang="he" dir="rtl"><Body style={{ backgroundColor: "#f4efe7", fontFamily: "Arial, sans-serif", color: "#2d1f14" }}><Container style={{ backgroundColor: "#fffaf3", padding: "24px", border: "1px solid #c8a86b" }}><Heading>הזמנה חדשה מהאתר</Heading><Text><strong>שם:</strong> {props.customerName}</Text><Text><strong>טלפון:</strong> {props.phone}</Text><Text><strong>סוג הזמנה:</strong> {props.orderType === "delivery" ? "משלוח" : "איסוף עצמי"}</Text><Text><strong>כתובת:</strong> {props.address || "לא הוזנה"}</Text><Text><strong>הערות:</strong> {props.notes || "ללא"}</Text><Section><Heading as="h2">פרטי ההזמנה</Heading>{props.items.map((item) => <Text key={item.name}>• {item.name} — כמות: {item.quantity}</Text>)}</Section></Container></Body></Html>; }
