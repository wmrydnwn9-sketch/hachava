import Link from 'next/link';
import { createServerSupabase } from '@/lib/supabase/server';
export async function SiteFooter(){ const supabase = await createServerSupabase(); const { data: settings } = await supabase.from('site_settings').select('*').single(); return <footer className="relative z-10 mt-16 border-t border-amber-200/15 bg-black/40"><div className="mx-auto grid max-w-7xl gap-8 px-4 py-10 md:grid-cols-3"><div><h3 className="mb-3 text-xl font-bold gold-text">החווה</h3><p className="leading-7 text-stone-200">קצביית פרימיום כשרה עם שירות אישי, טריות גבוהה ואווירה חמה מהכפר.</p></div><div><h3 className="mb-3 text-xl font-bold gold-text">צור קשר</h3><div className="space-y-2 text-stone-200"><p><a className="underline" href={`tel:${settings?.phone}`}>{settings?.phone}</a></p><p><a className="underline" href={settings?.whatsapp_url}>וואטסאפ</a></p><p><a className="underline" href={`mailto:${settings?.email}`}>{settings?.email}</a></p><p>{settings?.address}</p></div></div><div><h3 className="mb-3 text-xl font-bold gold-text">קישורים</h3><div className="flex flex-col gap-2 text-stone-200"><Link href="/products">מוצרים</Link><Link href="/order">הזמנה</Link><Link href="/about">אודות</Link><Link href="/contact">צור קשר</Link></div></div></div></footer>; }
