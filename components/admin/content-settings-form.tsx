"use client";
import { useState } from 'react';
export function ContentSettingsForm({ settings }: { settings: any }) {
  const [form, setForm] = useState(settings); const [saving, setSaving] = useState(false);
  async function save() { setSaving(true); const response = await fetch('/api/admin/content', { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(form) }); setSaving(false); if (!response.ok) return alert('שגיאה בשמירת התוכן'); alert('התוכן נשמר בהצלחה'); }
  return <div className="rustic-card rounded-[2rem] p-6 md:p-8"><div className="grid gap-4 md:grid-cols-2"><input value={form.business_name || ''} onChange={(e) => setForm({ ...form, business_name: e.target.value })} placeholder="שם העסק" className="input-base" /><input value={form.slogan || ''} onChange={(e) => setForm({ ...form, slogan: e.target.value })} placeholder="סלוגן" className="input-base" /><input value={form.phone || ''} onChange={(e) => setForm({ ...form, phone: e.target.value })} placeholder="טלפון" className="input-base" /><input value={form.whatsapp_url || ''} onChange={(e) => setForm({ ...form, whatsapp_url: e.target.value })} placeholder="קישור וואטסאפ" className="input-base" /><input value={form.email || ''} onChange={(e) => setForm({ ...form, email: e.target.value })} placeholder="אימייל" className="input-base" /><input value={form.instagram_url || ''} onChange={(e) => setForm({ ...form, instagram_url: e.target.value })} placeholder="קישור אינסטגרם" className="input-base" /><input value={form.address || ''} onChange={(e) => setForm({ ...form, address: e.target.value })} placeholder="כתובת" className="input-base" /><input value={form.map_embed_url || ''} onChange={(e) => setForm({ ...form, map_embed_url: e.target.value })} placeholder="קישור מפה להטמעה" className="input-base" /><input value={form.hero_title || ''} onChange={(e) => setForm({ ...form, hero_title: e.target.value })} placeholder="כותרת ראשית" className="input-base md:col-span-2" /><textarea value={form.hero_subtitle || ''} onChange={(e) => setForm({ ...form, hero_subtitle: e.target.value })} placeholder="טקסט פתיחה" rows={4} className="input-base md:col-span-2" /><input value={form.about_title || ''} onChange={(e) => setForm({ ...form, about_title: e.target.value })} placeholder="כותרת אודות" className="input-base md:col-span-2" /><textarea value={form.about_body || ''} onChange={(e) => setForm({ ...form, about_body: e.target.value })} placeholder="טקסט אודות" rows={6} className="input-base md:col-span-2" /></div><button onClick={save} disabled={saving} className="gold-btn mt-6 rounded-full px-6 py-3 font-black">{saving ? 'שומר...' : 'שמירת שינויים'}</button></div>;
}
