'use client';
import Link from 'next/link';
import { useEffect, useState } from 'react';
export function PromoModal({ promotion }: { promotion: any }){ const [open,setOpen]=useState(false); useEffect(()=>{ if(!promotion) return; const seen=sessionStorage.getItem(`promo:${promotion.id}`); if(!seen){ setOpen(true); sessionStorage.setItem(`promo:${promotion.id}`,'1'); } },[promotion]); if(!promotion || !open) return null; return <div className="fixed inset-0 z-[60] flex items-center justify-center bg-black/75 p-4"><div className="rustic-card w-full max-w-xl rounded-4xl p-6"><h2 className="mb-3 text-3xl font-black gold-text">{promotion.title_he}</h2><p className="mb-6 leading-8 text-stone-100">{promotion.description_he}</p><div className="flex flex-wrap gap-3"><Link href="/promotions" className="gold-btn rounded-full px-5 py-2 font-bold">לצפייה במבצעים</Link><button onClick={()=>setOpen(false)} className="gold-outline-btn rounded-full px-5 py-2">סגירה</button></div></div></div>; }
